{
  "name": "sunday-fantasy-hq",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173 --directory dist",
    "test": "node --test tests/*.test.js",
    "service": "node --env-file-if-exists=.env server/main.js",
    "mcp": "node --env-file-if-exists=.env server/mcp.js"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
