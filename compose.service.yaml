services:
  sunday:
    build:
      context: .
      dockerfile: Dockerfile.service
    env_file: .env
    environment:
      SUNDAY_HOST: 0.0.0.0
    ports:
      - "127.0.0.1:4173:4173"
    volumes:
      - sunday-data:/app/data
    restart: unless-stopped
volumes:
  sunday-data:
